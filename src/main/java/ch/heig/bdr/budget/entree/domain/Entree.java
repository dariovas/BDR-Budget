package ch.heig.bdr.budget.entree.domain;

import ch.heig.bdr.budget.bdgt.domain.Budget;

public class Entree extends Budget {
    public Entree(Long id, double montant) {
        super(id, montant);
    }
}
