package ch.heig.bdr.budget.sortie.domain;

import ch.heig.bdr.budget.bdgt.domain.Budget;

public class Sortie extends Budget {
    public Sortie(Long id, double montant) {
        super(id, montant);
    }
}
